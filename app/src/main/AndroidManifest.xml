<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mac.crossfitcoach">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name="com.example.mac.crossfitcoach.screens.sensor_placement.SensorPlacementActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screens.ankle_connection.AcceptConnectionActivity" />
        <activity android:name="com.example.mac.crossfitcoach.screens.log.LogActivity" />
        <activity android:name=".screens.main.MainMenuActivity" />
        <activity android:name="com.example.mac.crossfitcoach.screens.workout_done.WorkoutDoneActivity" />
        <activity android:name=".screens.exercise_list.ExerciseListActivity" />
        <activity android:name="com.example.mac.crossfitcoach.screens.record_session.RecordExerciseActivity"
            android:keepScreenOn="true"/>

    </application>
</manifest>